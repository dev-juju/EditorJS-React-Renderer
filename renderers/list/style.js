import { MARGIN, fontFamily } from '../../style/template';

const listOutputStyle = {
  container: {
    margin: MARGIN,
    fontFamily
  },
  listItem: {
    fontFamily
  },
};

export default listOutputStyle;
